# Notes

Deliberately unrelated to any build — edit this file, rebuild `crates/app`,
and watch the build step memo-hit: the covered closure is `crates/app` +
`crates/greeting`, and `docs/` is outside it.
